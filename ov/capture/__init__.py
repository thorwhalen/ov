"""Capture layer: drive a browser and record behavioral + static streams.

The public entry point is :class:`~ov.capture.session.CaptureSession`; persisted
state lives in :class:`~ov.capture.stores.CaptureStore`. Probes are registered
plugins (see :mod:`ov.capture.probes`).
"""
